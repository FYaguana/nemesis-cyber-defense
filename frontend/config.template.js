// Generado en runtime por docker-entrypoint-nemesis.sh a partir de la
// variable de entorno NEMESIS_API_URL. NO editar config.js a mano: se
// regenera cada vez que arranca el contenedor.
window.NEMESIS_API_URL = "${NEMESIS_API_URL}";
